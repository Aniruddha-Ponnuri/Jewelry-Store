'use client'

// LoginRedirect is now handled directly in the login page
// This component is kept for backward compatibility but does nothing
export default function LoginRedirect() {
  return null
}
